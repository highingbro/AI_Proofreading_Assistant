streamlit
pytest
# 不锁版本，但 1.27.2.2 起 Page.get_texttrace() 有引用计数 bug（每个 span 少给 None 加
# 一次引用，会让解释器 Fatal none_dealloc 硬崩），由 core/parser/_glyph_repair.py 自行
# 补偿，详见该文件 docstring。**别为了绕开那个 bug 把版本降到 1.26.x**：实测降级会改变
# PyMuPDF 自己的 block 切分，79期有 3 句退回词中断行、句中换行指标 5237→5239，
# 踩坏 _paragraphs.py 那套照 1.28 切分标定的段落合并。
pymupdf
python-docx
paddleocr
paddlex[ocr]==3.7.2

requests

# PaddlePaddle 
# 「PaddlePaddle 安装」一节：
#  - 有 NVIDIA GPU：装 GPU 版，需按本机 CUDA 版本
#    选择对应下载源，例如 CUDA 12.6：
#      pip install paddlepaddle-gpu==3.3.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#  - 纯 CPU：pip install paddlepaddle==3.3.1
#    （CPU 下 core/parser.py 会自动禁用 MKL-DNN 以规避已知崩溃，速度较慢）

openpyxl
